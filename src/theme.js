import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: "#00bcd4",
        }
    },
    background: {
        default: "#fafafa",
    }
})

export default theme;